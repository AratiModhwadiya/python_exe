"""
Traceback (most recent call last):
  File "C:\Users\Arti\PycharmProjects\pythonProject\module1.py", line 60, in <module>
    no = int(input('Enter No {}: '.format(i + 1)))
ValueError: invalid literal for int() with base 10:

ValueError: invalid literal for int() with base 10:
                                                             ^
SyntaxError: (unicode error) 'unicodeescape' codec can't decode bytes in position 46-47: truncated \UXXXXXXXX escape"""
"""str object is not callable()
AttributeError: 'product' object has no attribute 'values'
type object 'product' has no attribute 'name'
>=' not supported between instances of 'product' and 'product
nonetype object is not callable


        """obj = self
        while(obj.parent!=None):
            self.display_name=f'{obj.name}>{self.display_name}'
            obj=obj.name"""
        #if product.code in product.category:

        """for product in lst1 :
            print('Name : {}'.format(product.code))
            print('Name : {}'.format(product.name))"""
        """for product in lst1:
            print("Name:", self.name, "No_of_count:", self.no_of_count,product.name)"""
        # obj1 = self
        # for c in range(len(category_list)):
        # if category.name==category_list[c]:
        """for i in range(len(lst1)) :
            if product.name in lst1[i].name :
                print("Name:", self.name, "No_of_count:", self.no_of_count,self.name)
            else:
                break"""

    """while (category.name == product.name) :
        self.display_name = f'{category.name} > {self.display_name}'
        obj1 = category.name"""



"""
category_list.sort(key=lambda high_low : high_low.display_name)
        # def display_name1(self) :
        # code1 = input("enter code you want to groupby:")
        # print("Name:", self.name, "No_of_count:", self.no_of_count)
        """for i in range(len(lst1)) :
            if product.code in lst1[i].code :
                print(lst1[i].name)"""
        """for product in lst1 :
            for i in range(len(category_list)) :
                for p in range(len(lst1)) :
                    if category_list[i].name in lst1[p].name :
                        #product.display_category()
                        print(product.display_name)"""
        # print("name is: {}".format('name'))
        # print(self.display_name,self.name,self.display_category())

        # for j in range(len(category_list)) :

        # if category_list[j].name in lst1[i].name :
        # print(self.display_category())None and fruit ma fruit, ele ma ele
        # print(self.display_name)electronics ma electronics

        # print(self.display_name)
        # print(self.name)

        # code1 = input("enter code you want to search:")
        # print("Name:", self.name, "No_of_count:", self.no_of_count)
        """for i in range(len(category_list)) :
            # if category_list[i].no_of_count in product.code :
            while (category_list[i].no_of_count != None) :
                print(category_list[i].products)
                break"""